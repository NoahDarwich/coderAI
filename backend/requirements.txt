# Core Framework (required for basic testing)
# Note: Using specific versions that have pre-built wheels for Python 3.13
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database (required)
sqlalchemy>=2.0.0,<2.1.0
alembic>=1.13.0,<2.0.0

# Database Drivers
# SQLite async driver (works on Python 3.13)
aiosqlite>=0.19.0,<0.20.0

# PostgreSQL async driver (commented out for now, requires compilation)
# asyncpg>=0.29.0,<0.30.0

# LLM & AI (commented out for now - only needed for actual extraction)
# openai==1.10.0
# langchain==0.1.0
# langchain-openai==0.0.2

# Data Processing (commented out - requires compilation on Python 3.13)
# pandas>=2.1.0,<2.3.0
# openpyxl==3.1.2
# numpy>=1.26.0,<2.0.0

# Document Processing (commented out - only needed for file upload)
# PyMuPDF==1.23.8
# python-docx==1.1.0

# HTTP & Utilities (required)
httpx==0.26.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Future: Auth & Sessions (not implemented yet)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
# redis==5.0.1
